Humans h = new Humans();
String fName = h.FullName('meric', '2kol');
system.debug('FullName is ' + fName + '.');

integer age = h.age(date.newInstance(2013, 12, 17));
system.debug('Age is ' + age + '.');